spring:
  application:
    name: staracademybackend
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://admin:Admin%40123@cluster0.kcxaihb.mongodb.net/staracademy}
  servlet:
    multipart:
      max-file-size: -1
      max-request-size: -1
  config:
    import: optional:file:.env[.properties]

server:
  port: ${PORT:8080}

aws:
  s3:
    bucket-name: ${AWS_S3_BUCKET:star-academy-gallery}
    region: ${AWS_REGION:us-east-1}
  access-key: ${AWS_ACCESS_KEY:}
  secret-key: ${AWS_SECRET_KEY:}

security:
  jwt:
    secret: ${JWT_SECRET:very-long-and-secure-random-secret-key-for-star-academy-backend-jwt}
    expiration: 86400000

admin:
  username: ${ADMIN_USERNAME:admin}
  password: ${ADMIN_PASSWORD:admin123}
